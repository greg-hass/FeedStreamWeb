# Backend Environment Variables
# For local development only - use Dockge environment UI for production

NODE_ENV=production
PORT=3001

# Database (PostgreSQL)
# For local dev, replace 'changeme' with your actual password
# For Dockge deployment, this is set in the compose file using ${DB_PASSWORD}
DATABASE_URL=postgres://feedstream:changeme@localhost:5432/feedstream

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long

# Gemini API Key (get from Google AI Studio)
GEMINI_API_KEY=your-gemini-api-key-here

# CORS (allow your PWA domain)
CORS_ORIGIN=*

# Rate limiting
RATE_LIMIT_MAX=100

# NOTE: For Dockge deployment, you don't need this .env file.
# Set these variables in the Dockge UI instead:
# - DB_PASSWORD
# - JWT_SECRET
# - GEMINI_API_KEY
# - CORS_ORIGIN
# - RATE_LIMIT_MAX
